export default {
  title: 'Ticket Configuration',
  description: 'Manage ticket validation, harvesting and retry controls.',
  proxySettings: 'Default harvest proxy',
  loading: 'Loading ticket configuration…',
  reload: 'Reload',
  loadFailed: 'Failed to load ticket configuration',
  saveFailed: 'Save failed. Your changes are preserved; please retry.',
  policy: 'Enablement and models',
  credentialMode: 'Global ticket credential mode',
  credentialState: 'STATE only (legacy)',
  credentialCookieState: 'Cookie + STATE (experimental)',
  credentialCookie: 'Cookie only (experimental)',
  credentialModeHint: 'Accounts follow this mode unless overridden in account management. The legacy STATE flow no longer indicates reliable model recovery; validate Cookie modes for each account.',
  cookieTitle: 'Cookie updates and expiry',
  cookieTimingHint: 'Accounts inherit these values unless overridden. Update or revalidate under the selected strategy; an advance window of 0 disables early updates. Cookies without an actual expiry use this interval as a fallback. Cookie updates never extend actual STATE expiry.',
  cookieLengthHint: 'Cookie modes validate Cookies and the response model, without STATE tier-length or rejected-length rules. Cookie + STATE only requires a transferable STATE. Length settings below are preserved and resume in STATE mode.',
  cookieProtectionHint: 'Cookie modes do not reject or silence proxies based on STATE length. Other retry and cooldown policies still apply.',
  protectionTitle: 'Shared harvest protection',
  protectionEnabled: 'Enable proxy silence and account consecutive failure protection',
  protectionHint: 'A harvest HTTP 200 with a valid ticket header matching a configured length rejects the candidate and silences the registered proxy. The rejected account follows the independent retry policy below; other accounts still observe proxy silence. Reaching the account consecutive failure or pool round limit triggers a cooldown. Successful ticket issuance clears consecutive failures and waits. Model mismatch alone does not trigger silence. Ordinary traffic and existing valid tickets are unaffected.',
  protectionAutoHint: 'The enabled reject-and-silence rule overrides dynamic validation for new candidates matching these lengths. The legacy rejected-length list remains inactive in dynamic mode.',
  protectionLengths: 'Reject-and-silence lengths (comma separated, may be empty)',
  ipProtectionTitle: 'IP ticket failure protection',
  ipProtectionEnabled: 'Enable IP failure protection across accounts',
  ipProtectionHint: 'Independent of shared harvest protection. Cool down an IP when the number of distinct failed accounts reaches the threshold within the time window. Repeated failures from one account count once. Retry after cooldown; another threshold breach adds a round. Successful ticket issuance clears failed accounts and rounds.',
  ipProtectionIdentityHint: 'When enabled, choose a proxy from proxy management. Protection applies only to identified egress IPs. Cooldown and disabling restrict both ticket harvesting and business proxy use. Use the detected egress IP when available, otherwise the literal IP in the proxy address. Domain-only proxies with an unknown egress IP do not participate in aggregation.',
  ipProtectionPersistenceHint: 'Reaching the round limit disables the IP without automatic recovery. State is shared across instances. Turning this off does not clear existing cooldowns or disabled IPs, and turning it back on does not reset them.',
  rejectionRetryTitle: 'Retries after policy rejection',
  rejectionRetryHint: 'Applies to all ticket policy rejections, including ordinary rejected-length rules, even when shared harvest protection is disabled. Count consecutive rejections across models for each account, including the first rejection. Retry at a fixed interval before the limit. At the limit, wait for the cooldown, reset the count and retry. Successful ticket issuance clears the count. This policy takes priority over ordinary failure backoff and proxy silence, account consecutive failure protection or pool round waits caused by that rejection.',
  lengthMode: 'Ticket validation method',
  lengthModeAuto: 'Dynamic validation (no fixed length)',
  lengthModeStrict: 'Strict matching by subscription tier',
  lengthModeAutoHint: 'Validate the harvest model response without requiring a fixed length. The business egress verification switch above controls the second check. Tier rules, fallback length and rejected lengths below are preserved but inactive in dynamic mode. Switch back to strict matching to restore them.',
  lengthModeStrictHint: 'Tickets must match the account subscription tier target length and pass harvest model response validation. The business egress verification switch above controls the second check. Existing configurations without a validation method continue using strict matching.',
  enabled: 'Enable ticket harvesting',
  enabledHint: 'Only accounts included in harvesting participate. Turning this off stops probes and ticket injection.',
  failClosed: 'Pause model scheduling without a valid ticket',
  failClosedHint: 'Applies to STATE, Cookie + STATE, and Cookie modes, including account overrides. When enabled, an account without a valid ticket for the target model cannot receive requests for that model. When disabled, requests may be forwarded without a ticket.',
  verifyBusiness: 'Business egress verification',
  verifyBusinessHint: 'Controls verification before publishing new tickets: verify for the configured rounds when enabled, or only harvest when disabled. The revalidate-first update strategy still verifies old tickets. Re-enabling requires unverified tickets to be harvested and verified again.',
  businessVerificationRounds: 'Egress quality probe rounds',
  businessVerificationRoundsHint: 'The default 1 verifies once through the account business egress. With 2–10 rounds, keep the harvest egress, ticket, Cookie and Session fixed throughout verification. Publish only when every request succeeds and its response model matches the requested model. A different business egress receives one additional verification. Any failure stops and discards the candidate; the proxy pool tries another eligible node under its selection policy. Targets come from the harvest models below.',
  sessionMode: 'Harvest Session reuse scope',
  sessionModeRandom: 'Random per request (default)',
  sessionModeAccount: 'Fixed per account',
  sessionModeAccountModel: 'Fixed per account and model',
  sessionModeHint: 'Random creates a new Session for each request. Fixed modes reuse it within the current cycle: per account shares it across models, while per account and model isolates models. Failure counts are always independent per account and model. Any model reaching the threshold below rotates the Session for that scope; success clears only that model’s count. Restarts do not reset counts, and changing modes does not revoke existing valid tickets.',
  refreshStrategy: 'Automatic update strategy',
  refreshStrategyReplace: 'Harvest new tickets in advance',
  refreshStrategyRevalidate: 'Replace tickets after validation fails',
  refreshStrategyReplaceHint: 'Harvest new tickets at the configured interval and advance window without revalidating old tickets first. Keep using valid old tickets while harvesting, then publish new tickets under the verification rules above.',
  refreshStrategyRevalidateHint: 'Default: revalidate old tickets at the configured interval and advance window. Keep valid tickets and replace them after confirmed invalidation or expiry. Temporary network and rate-limit failures still follow retry rules.',
  refreshStrategyManualHint: 'Manual force harvesting always requests new tickets.',
  models: 'Harvest models',
  modelsHint: 'Select up to 32 models from the existing OpenAI text model catalog. Typing only searches the list. Previously configured models outside the catalog are preserved and can be removed.',
  selectModels: 'Search and select harvest models',
  selectTiers: 'Select one or more subscription tiers',
  existingSelection: '{value} (previously configured)',
  tiers: 'Subscription tier rules',
  tiersHint: 'Select subscription tiers from the catalog. Tags in one row share a target length. Defaults: Business Standard (Team) / Business 332, Pro 20x 292. Tiers used by other rules cannot be selected again. Existing matching scope and custom values are preserved.',
  addTier: 'Add rule',
  tier: 'Subscription tier',
  length: 'Target length',
  remove: 'Remove',
  removeTier: 'Remove tier rule {tier}',
  noTiers: 'No tier rules. All accounts use the fallback length.',
  fallbackHint: 'Used when no rule matches or the subscription tier is unknown. Range: 16–8192.',
  rejectedLengths: 'Rejected ticket lengths',
  rejectedHint: 'Comma separated; leave empty for no explicit rejected lengths. Cannot overlap any target length.',
  harvest: 'Harvesting and updates',
  poolCapacityHint: 'Store tickets separately for each account and model. Default: 5. Background harvesting stops when full and replenishes low or expiring stock. Valid tickets are reusable; the count is not a request quota.',
  proxyFailureThresholdHint: 'Default: 3. Harvesting and verification each keep their proxy and count consecutive failures independently per account and model. Stage success clears only that model’s count. With a threshold of 20, one Sol failure and two Astra failures are not added together; another eligible proxy is selected only when one model reaches 20. Fixed Sessions use the same threshold, with at most one failure per model per logical harvest. Temporary waits do not count; invalid proxy configuration allows reselection.',
  harvestHint: 'All times are in seconds. At the update window, the selected strategy harvests new tickets directly or revalidates old tickets first. An advance window of 0 disables early updates. Interval changes apply after the next harvest or successful revalidation. When STATE expiry cannot be parsed, the interval remains the fallback lifetime. Cookie updates never extend actual STATE expiry.',
  retry: 'Retries and cooldowns',
  retryHint: 'Ordinary failures are counted per account, model and credential; success resets them. A limit of 0 keeps retrying at the final delay. A nonzero limit triggers the exhausted cooldown, then a new cycle. Disabling harvesting, daily cooldowns and explicit cancellations do not count as failures.',
  backoff: 'Wait after each failure (seconds)',
  backoffHint: 'Up to 16 nondecreasing integer delays, each 1–86400 seconds. Failures beyond the list use its final delay. Example: 30, 60, 300, 600, 1200, 1800.',
  retryAfter: 'Honor upstream Retry-After',
  retryAfterHint: 'For 401 / 403 / 429 responses, extend the cooldown if the upstream asks for a longer wait. Never shorten the configured cooldown.',
  range: 'Integer range: {min}–{max}',
  applyHint: 'Saves apply immediately on this instance; other instances refresh in about 5 seconds. Existing ticket lifecycle rules remain in effect; protection does not revoke existing valid tickets in bulk. Shared budgets and silence span instances, and disabling protection does not immediately clear existing deadlines. Daily cooldown windows remain in account settings.',
  save: 'Save configuration',
  saving: 'Saving…',
  saved: 'Ticket configuration saved.',
  updateFields: {
    cookie_ttl_seconds: 'Default Cookie update interval (seconds)',
    cookie_refresh_before_seconds: 'Cookie update ahead (seconds)',
    ttl_seconds: 'STATE update interval (seconds, STATE mode only)',
    refresh_before_seconds: 'STATE update ahead (seconds, STATE mode only)'
  },
  fields: {
    cookie_ttl_seconds: 'Default Cookie revalidation interval (seconds)',
    cookie_refresh_before_seconds: 'Cookie revalidation ahead (seconds)',
    pool_capacity: 'Ticket capacity per account and model',
    proxy_failure_threshold: 'Per-model proxy / Session consecutive failure threshold',
    business_verification_rounds: 'Egress quality probe rounds',
    proxy_silence_seconds: 'Proxy silence (seconds)',
    max_pool_rounds: 'Pool traversal round limit',
    max_account_attempts: 'Account consecutive failure limit across models',
    account_cooldown_seconds: 'Cooldown at account failure limit (seconds; success resets)',
    proxy_ip_failure_account_threshold: 'Distinct failed accounts per proxy IP before cooling',
    proxy_ip_failure_window_seconds: 'IP failure time window (seconds)',
    proxy_ip_cooldown_seconds: 'Proxy IP cooldown after the failed-account threshold (seconds)',
    proxy_ip_max_rounds: 'Consecutive proxy IP cooldown rounds before disabling',
    rejection_retry_interval_seconds: 'Policy rejection retry interval (seconds)',
    rejection_retry_max_attempts: 'Consecutive policy rejection limit (including first)',
    rejection_retry_cooldown_seconds: 'Cooldown at policy rejection limit (seconds)',
    target_length: 'Unknown-tier fallback length',
    ttl_seconds: 'STATE revalidation interval (seconds, STATE mode only)',
    refresh_before_seconds: 'STATE revalidation ahead (seconds, STATE mode only)',
    harvest_probe_interval_seconds: 'Background probe interval (seconds)',
    harvest_attempt_timeout_seconds: 'Request timeout (seconds)',
    harvest_concurrency: 'Harvest concurrency',
    retry_max_attempts: 'Consecutive failure limit per cycle',
    retry_exhausted_cooldown_seconds: 'Cooldown after retries are exhausted (seconds)',
    auth_cooldown_seconds: '401 / 403 credential cooldown (seconds)',
    rate_limit_cooldown_seconds: '429 rate-limit cooldown (seconds)'
  },
  errors: {
    refreshStrategy: 'Select harvesting new tickets in advance or replacing them after validation fails.',
    credentialMode: 'Select STATE only, Cookie + STATE, or Cookie only.',
    cookieRefresh: 'Cookie update or revalidation ahead must be shorter than the default interval.',
    verifyBusiness: 'Business egress verification must be enabled or disabled.',
    sessionMode: 'Select random per request, fixed per account, or fixed per account and model.',
    ipProtectionEnabled: 'IP failure protection must be enabled or disabled.',
    protectionLengths: 'Enter up to 32 unique reject-and-silence lengths between 16 and 8192.',
    protectionOverlap: 'When protection is enabled, reject-and-silence lengths cannot match a strict fallback or tier target length.',
    lengthMode: 'Select dynamic validation or strict matching by subscription tier.',
    range: '{field} must be an integer between {min} and {max}.',
    refresh: 'Update or revalidation ahead must be shorter than its interval.',
    models: 'Select 1–32 unique models. Previously configured names must be up to 160 characters without whitespace.',
    selectTier: 'Select at least one subscription tier per rule, or remove the empty rule.',
    tiers: 'At most 32 subscription tier rules are allowed.',
    tierLength: 'Each tier target length must be an integer between 16 and 8192.',
    aliases: 'Each tier supports up to 16 aliases.',
    tierName: 'Tier names and aliases must be 1–80 letters, numbers, spaces, underscores or hyphens, including a letter or number.',
    duplicateTier: 'Duplicate tier or alias. Case, spaces, underscores and hyphens are ignored when matching.',
    backoff: 'Enter 1–16 nondecreasing integer retry delays, each between 1 and 86400 seconds.',
    rejected: 'Enter up to 32 unique integer rejected lengths between 16 and 8192.',
    overlap: 'Rejected lengths cannot match the fallback length or any tier target length.'
  }
}
